fastapi
uvicorn[standard]
python-multipart
Pillow

# server/jupyter_gate.py(주피터 앞 로그인 게이트)가 직접 import한다. uvicorn[standard]가
# 이미 끌어오지만, 직접 쓰는 만큼 명시적으로 적어둔다.
websockets

# mcp_server.py (nightshift 잡 큐를 MCP 도구로 감싸는 별도 프로세스)가 쓴다.
fastmcp
httpx

# templates/claude_write.py(claude_writer 드라이버)가 Claude API를 부르는 데 쓴다.
anthropic

# server/video_edit.py(영상 자르기/이어 붙이기)가 쓰는 ffmpeg 바이너리 — 시스템에 ffmpeg가 없을 때의 대비책이다.
imageio-ffmpeg
